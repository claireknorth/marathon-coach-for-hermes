# Training Calendar Config

Use the dedicated training-plan calendar when available, not a main calendar
that may contain copied or transformed events.

Store the calendar ID in:

```text
~/.hermes/marathon/athlete-profile.yaml
```

Useful event fields:

- summary
- description
- start
- end
- location

If the calendar is unavailable, use `references/runna-plan-inference.md`.
